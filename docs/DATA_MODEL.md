# Advisory data model

Patterns, associations, gaps, staged artifacts, proposals, and audit records are disposable MACES state. They are not Canon. Promotion creates proposals only; no code path writes Obsidian or other canonical memory.

Influence reads only bounded pattern, edge, and open-gap queries. It never reads staged artifact content.
