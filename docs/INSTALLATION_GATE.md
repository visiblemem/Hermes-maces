# Installation gate

Do not deploy MACES broadly until CI passes the unit, concurrency, privacy, and plugin-load tests. Begin with one profile in shadow mode and review the first fifty high-weight concepts before enabling influence.
